# AGENTS.md — AI 开发守则（机器可读，优先级最高）

> 你是 AI 编码助手。本文件是本项目对你的硬性约束，**先于用户的口头要求执行**。
> 人类维护者不会逐条检查你的工作，守门的是 lint 和这些规则。

## 项目一句话

纯前端零依赖修仙文字游戏。内容全部在 `assets/data.js`（纯数据），引擎在 `assets/loop.js` 等文件里。

## 每次开工前 MUST

1. **先拉分支**：`git checkout -b <类型>/<主题>`，类型 ∈ `content|feat|fix|balance|docs`。NEVER 直接在 main 上提交。
2. 通读本文件 + `tools/文案规范.md`。加事件再读 `CONTRIBUTING.md` 的 Schema 一节；改引擎再读 `docs/使用教程.md` 第二章。

## 提交前 MUST（不满足不许 commit）

```bash
node tools/lint.js   # 必须 0 错误（警告可以，但要向用户说明）
```

改了数值或引擎：再跑 `node tools/sim.js`。改了秘境：再跑 `node tools/dgtest.js`。

## 内容铁律（加/改事件时）

- `data.js` 只许是数据，NEVER 写入逻辑；引擎文件 NEVER 出现具体事件 id 的特判。
- 事件 id 前缀：炼气无前缀 / `zb_` 筑基 / `jd_` 结丹 / `mj_<id>_in` 秘境入口 / `mjw_<id>` 秘境传闻。**已存在的 id 永不改名**（存在玩家存档里）。
- 每个 choice 事件至少留一个**不带 cond 的兜底选项**；大额扣灵石选项 MUST `cond.inv` 门控。
- 抉择优先 `outcomes` 加权随机，NEVER 写"固定哪个好"的明牌选项。
- flag 和物品名只用 `tools/文案规范.md` 词汇表里的现成词；新词先向用户确认。
- 结丹三宝（玄冰魄/炎髓晶/雷灵枝）NEVER 由普通事件发放，只许秘境产出。
- 成熟内容事件 MUST 带 `minAge`。
- 数值档位遵守 `tools/文案规范.md` 末尾的经济档位表，不超标。

## 文案红线（lint 会拦，但你该自觉）

- 禁词：一世、前世、来世、轮回、投胎、享年、重生。
- 死亡只写：坐化、兵解、道消。
- 凡人流语感：短句、写"事"不写"情"；NEVER 出现 AI 腔和网络烂梗（"贵但值""破防了""YYDS"……）。好/坏范例见 `tools/文案规范.md`。

## 引擎与存档铁律（改代码时）

- 存档字段**只增不删**；读旧档新字段一律 `||0` / `||{}` 默认值；存档版本号 `v:1` NEVER 改。
- 属性 clamp 0–120、灵石 clamp ≥0 已有，但大额扣款靠 cond 门控，NEVER 靠 clamp 兜底。
- 突破成功率一律 `Math.min(0.95, …)` 封顶。
- 剧情关键物品（结金丹、三宝）获得时无视背包格限；普通法宝受 12 格限制。
- 日志 DOM 保留最近 300 条上限不许去掉。

## 提交信息格式

`<类型>: <中文描述>`，类型 ∈ `feat|fix|content|balance|docs|chore`。描述写原因和范围，不写"更新代码"这类废话。一批事件一个 commit。

## 主动提建议与发问

每次交付后 MUST 主动向用户提下一步建议，并发问确认方向。NEVER 干等用户自己想起来。

- **问境界**："接下来想继续开发哪个境界？"——选项列出当前内容最薄弱的 2–3 个境界。境界池：炼气 / 筑基 / 结丹 / 元婴 / 化神 / 化凡 / 神识 / 全艺 / 跨界。
- **问事件**："有哪个事件想拓展加深？"——选项列出本次改动涉及的事件 id 或主题 2–3 个 + "暂时没有"。
- 每次发问 MUST 列出编号选项供用户直接选，NEVER 只抛开放式问题。选项控制在 3–5 个，最后留一个"其他/都没有"的出口。
- 建议要具体：说"筑基事件只有 N 条，比结丹少一半"，NEVER 说"建议充实内容"这种空话。

## 交付时向用户报告

1. 改了哪些文件、加了哪些事件/机制（一句话一个）；
2. lint / sim 的实际输出结论；
3. 哪些分支需要用户浏览器实测后再合并。
